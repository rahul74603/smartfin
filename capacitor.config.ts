import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rahul74603.smartfintool',
  appName: 'SmartFintool',
  webDir: 'dist'
};

export default config;
